import { FindUsersPipe } from './find-users.pipe';

describe('FindUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FindUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
